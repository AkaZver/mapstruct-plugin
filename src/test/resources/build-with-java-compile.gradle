plugins {
    id 'com.github.akazver.mapstruct'
    id 'java'
}

allprojects {
    afterEvaluate {
        println "Compiler args -> ${tasks.withType(JavaCompile).getByName('compileJava').options.allCompilerArgs}"
    }
}
